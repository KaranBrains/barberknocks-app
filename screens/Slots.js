import React from "react";
import { StyleSheet, Text } from "react-native";

function Slots({ navigation }) {
  return <Text>SLOTS</Text>;
}

const styles = StyleSheet.create({});

export default Slots;
